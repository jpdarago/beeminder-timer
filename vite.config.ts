/// <reference types="vitest/config" />
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import { VitePWA } from 'vite-plugin-pwa'

// https://vite.dev/config/
export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'autoUpdate',
      workbox: {
        globPatterns: ['**/*.{js,css,html,svg,mp3,png,ico}'],
      },
      manifest: {
        name: 'Beeminder Timer',
        short_name: 'Bee Timer',
        description: 'Focus sessions, logged as datapoints to Beeminder.',
        theme_color: '#4c2e68',
        background_color: '#4c2e68',
        display: 'standalone',
        scope: '/timer/',
        start_url: '/timer/',
        icons: [
          {
            src: 'pwa-64x64.png',
            sizes: '64x64',
            type: 'image/png',
          },
          {
            src: 'pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png',
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png',
          },
          {
            src: 'maskable-icon-512x512.png',
            sizes: '512x512',
            type: 'image/png',
            purpose: 'maskable',
          },
        ],
      },
    }),
  ],
  base: "/timer/",
  test: {
    environment: 'jsdom',
  },
})
